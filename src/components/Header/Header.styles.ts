import { makeStyles } from '@mui/styles';

const useStyles = makeStyles(() => ({
  desktopHeader: {
    display: 'flex',
    justifyContent: 'space-between',
    padding: '9px 20.21vw 8px 19.47vw',
  },
  mobileHeader: {},
}));

export default useStyles;
